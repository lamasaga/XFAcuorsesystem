# 开发启动方式完全指南

## 核心规律：记住这张图

```
┌─────────────────────────────────────────────────────────────┐
│  你想启动的东西           →   使用的工具     →   典型命令    │
├─────────────────────────────────────────────────────────────┤
│  单个脚本文件             →   解释器直接跑   →   py xxx.py   │
│  包管理器里的脚本命令     →   包管理器       →   npm run dev │
│  框架内置命令             →   框架 CLI       →   next dev    │
│  编译好的二进制程序       →   直接执行       →   ./app.exe   │
│  容器化应用               →   Docker         →   docker up   │
└─────────────────────────────────────────────────────────────┘
```

---

## 一、为什么命令长得不一样？本质差异

### 1. 解释型语言（Python、Node.js）

代码是人类能看懂的文本，**每次运行都需要一个翻译官（解释器）实时翻译成机器语言**。

```
你的代码 (.py/.js)  →  解释器 (python/node)  →  运行结果
```

**所以必须带上解释器名字：**

```bash
py hello.py          # Python 解释器去执行 hello.py
node server.js       # Node.js 解释器去执行 server.js
```

### 2. 编译型语言（Go、Rust、C++）

代码先被**一次性翻译成机器可执行文件**，之后直接运行文件，不需要解释器。

```
你的代码 (.go/.rs)  →  编译器编译  →  可执行文件 (.exe)  →  双击或 ./app 运行
```

```bash
go build             # 编译出可执行文件
go run main.go       # 编译+运行（开发时常用，等价于 py xxx.py）

# Rust
cargo run            # 编译+运行
cargo build          # 只编译
./target/debug/app   # 运行编译好的文件
```

### 3. 包管理器（npm/pnpm/yarn/pip/poetry）

这些工具**不是运行器，而是"管家"**。它们管理依赖、定义脚本命令。

```
为什么 npm run dev？

npm      = 管家（我知道项目里有什么工具）
run      = 执行一个预定义好的脚本
dev      = 脚本名字（定义在 package.json 里）
```

打开 `frontend/package.json`：

```json
{
  "scripts": {
    "dev": "vite",       ← npm run dev 实际上就是在执行 "vite"
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

**`npm run` 只是替身，真正干活的是后面的工具。**

同理：

```bash
# Python 的包管理器
pip install -r requirements.txt    # pip 是 Python 的包管家
poetry run python main.py          # poetry 是更现代的 Python 管家

# Java 的包管理器
mvn spring-boot:run                # Maven 管家执行 spring-boot 插件
./gradlew bootRun                  # Gradle 管家执行启动任务
```

---

## 二、按技术栈分类速查表

### Python 生态

| 场景 | 命令 | 说明 |
|------|------|------|
| 运行单个脚本 | `py xxx.py` | 最基础 |
| 运行模块 | `python -m http.server` | `-m` 表示运行某个模块 |
| FastAPI/Flask | `uvicorn main:app --reload` | Uvicorn 是 ASGI 服务器，专门跑 Web 应用 |
| Django | `python manage.py runserver` | Django 自带开发服务器 |
| 虚拟环境激活 | `.venv\Scripts\activate` | Windows 下激活虚拟环境 |
| 虚拟环境运行 | `.venv\Scripts\python.exe xxx.py` | 不激活也能直接指定虚拟环境的 Python |

**为什么需要虚拟环境？**

Python 的项目依赖装在全球环境里会互相冲突。`.venv` 就是一个**隔离的小房间**，每个项目一个房间，互不影响。

```
全局 Python
  ├── 项目 A（需要 Flask 1.0）
  └── 项目 B（需要 Flask 2.0）  ← 冲突！

虚拟环境后：
  ├── 项目 A/.venv（Flask 1.0）
  └── 项目 B/.venv（Flask 2.0）  ← 各自独立
```

### Node.js 生态（前端 + 后端）

| 场景 | 命令 | 说明 |
|------|------|------|
| 安装依赖 | `npm install` | 根据 package.json 下载所有依赖到 node_modules |
| 运行脚本 | `npm run xxx` | 执行 package.json scripts 里定义的命令 |
| 直接运行 | `node server.js` | 不通过 npm，直接用 node 解释器执行 |
| 临时工具 | `npx create-react-app my-app` | 不安装全局，临时下载并执行一个工具 |
| 开发服务器 | `npm run dev` | 通常是启动 Vite/Webpack/Next 的开发服务器 |
| 生产构建 | `npm run build` | 打包成静态文件，放在 dist/ 目录 |
| 预览生产包 | `npm run preview` | 用生产模式预览构建结果 |

**常见前端框架的启动命令：**

```bash
# Vue 3 + Vite（你的项目用的这个）
npm run dev              # 启动开发服务器

# React + Vite
npm run dev

# Next.js（React 全栈框架）
npm run dev              # 启动 Next.js 开发服务器
# 或直接用框架 CLI：
next dev

# Nuxt.js（Vue 全栈框架）
npm run dev
# 或：
nuxt dev

# Angular
ng serve

# SvelteKit
npm run dev
```

**为什么有的框架有自己的 CLI（如 `next dev`）？**

这些框架不只是启动一个服务器，还要做路由解析、SSR 渲染、热更新、编译转换等复杂工作，所以它们封装了自己的命令行工具。

### Java 生态

| 场景 | 命令 | 说明 |
|------|------|------|
| Maven 运行 | `mvn spring-boot:run` | Maven + Spring Boot 插件 |
| Gradle 运行 | `./gradlew bootRun` | Gradle 的启动任务 |
| 直接运行 JAR | `java -jar app.jar` | 运行已经打包好的可执行 JAR |
| 编译 | `mvn clean install` | 编译并打包 |

### Go 生态

| 场景 | 命令 | 说明 |
|------|------|------|
| 直接运行 | `go run main.go` | 编译并运行（开发时用） |
| 编译 | `go build` | 编译出 .exe 可执行文件 |
| 运行编译结果 | `./myapp.exe` | 直接执行二进制文件 |

### Rust 生态

| 场景 | 命令 | 说明 |
|------|------|------|
| 运行 | `cargo run` | 编译+运行 |
| 编译 | `cargo build` | 调试版本 |
| 发布编译 | `cargo build --release` | 优化后的生产版本 |

### Docker 生态

| 场景 | 命令 | 说明 |
|------|------|------|
| 运行单个容器 | `docker run -p 8080:80 nginx` | 运行 nginx 镜像，端口映射 |
| 启动 Compose | `docker-compose up` | 启动 docker-compose.yml 定义的所有服务 |
| 后台运行 | `docker-compose up -d` | `-d` = detached，后台运行 |
| 查看日志 | `docker-compose logs -f` | 实时查看日志 |
| 停止 | `docker-compose down` | 停止并删除容器 |

---

## 三、命令结构拆解：你看到的命令到底在说什么？

### 例子 1：`py xxx.py`

```
py              = 解释器（翻译官）
xxx.py          = 要翻译的代码文件
─────────────────────────────────
合起来：让 Python 解释器执行这个脚本
```

### 例子 2：`npm run dev`

```
npm             = Node 的包管理器管家
run             = 我要执行一个预定义的脚本
dev             = 脚本的名字（在 package.json 里定义的）
─────────────────────────────────
合起来：让 npm 去 package.json 里找到 "dev" 对应的命令并执行
```

等价展开：

```bash
npm run dev
    ↓
# 实际执行的是 package.json 里：
"dev": "vite"
    ↓
# 等价于直接运行：
npx vite
```

### 例子 3：`.venv\Scripts\python.exe -m uvicorn app.main:app --reload`

拆解：

```
.venv\Scripts\python.exe    # 使用虚拟环境里的 Python 解释器
-m uvicorn                  # 运行 uvicorn 这个 Python 模块
app.main:app                # 导入路径：app/main.py 里的 app 对象
--reload                    # 开启热重载（代码改了自动重启）
--host 0.0.0.0              # 允许局域网访问
--port 8001                 # 监听 8001 端口
```

### 例子 4：`npx create-next-app@latest`

```
npx             = npm 的临时执行工具（Node Package eXecute）
create-next-app = 要执行的包名
@latest         = 版本标签，用最新版
─────────────────────────────────
合起来：临时下载 create-next-app 工具并执行，执行完就删掉
        不用全局安装这个工具
```

---

## 四、如何快速判断一个项目该怎么启动？

### 第一步：看根目录文件

| 看到什么文件 | 属于什么技术栈 | 启动方式 |
|-------------|--------------|----------|
| `package.json` | Node.js 项目 | `npm install` → `npm run dev` |
| `requirements.txt` / `pyproject.toml` | Python 项目 | 装依赖 → `py xxx.py` 或 `uvicorn` |
| `pom.xml` | Java Maven 项目 | `mvn spring-boot:run` |
| `build.gradle` | Java Gradle 项目 | `./gradlew bootRun` |
| `go.mod` | Go 项目 | `go run main.go` |
| `Cargo.toml` | Rust 项目 | `cargo run` |
| `Dockerfile` / `docker-compose.yml` | Docker 项目 | `docker-compose up` |
| `vite.config.js` | Vite 前端项目 | `npm run dev` |
| `next.config.js` | Next.js 项目 | `npm run dev` 或 `next dev` |
| `manage.py` | Django 项目 | `python manage.py runserver` |

### 第二步：看 README

正经项目都会在 README 里写：

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### 第三步：看 package.json / pyproject.toml 里的 scripts

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview"
  }
}
```

这些就是项目预定义的快捷命令。

---

## 五、常见困惑解答

### Q1：为什么有时候用 `py`，有时候用 `python`，有时候用 `python3`？

```
Windows:   py        = 系统注册的 Python 启动器（推荐在 Windows 用）
           python    = 直接调用 python.exe

Linux/Mac: python3   = 系统自带的 Python 3
           python    = 有时候指向 Python 2，有时候不存在
```

**结论：Windows 用 `py`，Linux/Mac 用 `python3`，不会错。**

### Q2：为什么 npm 命令有的要 `run`，有的不要？

```bash
npm install          # ❌ 不需要 run，这是 npm 内置命令
npm run dev          # ✅ 需要 run，dev 是自定义脚本
npm start            # ❌ 不需要 run，start/test 是 npm 内置别名
```

npm 内置命令（不用 run）：`install`、`start`、`test`、`publish`、`run` 本身
自定义脚本（必须用 run）：`dev`、`build`、`lint`、`format` 等

### Q3：`.exe` 文件为什么有时候双击就行，有时候需要命令行？

- **有图形界面的程序**（如浏览器、IDE）：双击就能运行
- **命令行程序**（如 Python、Node、编译器）：双击会闪退，因为它在命令行里输出内容，没有命令行窗口就看不到
- **服务器程序**（如你的后端）：需要在命令行里启动，才能看到日志输出

### Q4：环境变量 PATH 是什么？为什么有时候命令找不到？

```
PATH = 系统的一个"快捷方式列表"

当你在命令行输入 py 时：
1. 系统去 PATH 里列的所有文件夹里找 py.exe
2. 找到了就执行
3. 找不到就报错：'py' 不是内部或外部命令
```

安装 Node.js 时如果没有勾选 "Add to PATH"，就会导致 `npm` 命令找不到。

### Q5：开发服务器和生产服务器有什么区别？

```
开发模式（npm run dev / uvicorn --reload）
  ├── 热重载：代码改了自动刷新
  ├── 详细的错误提示
  ├── Source Map：方便调试
  └── 性能未优化，不适合给用户用

生产模式（npm run build / uvicorn 不带 --reload）
  ├── 代码压缩优化
  ├── 去掉调试信息
  ├── 性能最大化
  └── 适合正式部署
```

---

## 六、一条万能判断流程

```
拿到新项目 → 看根目录有什么标志性文件
              │
              ├── package.json → Node 项目 → npm install → npm run dev
              │
              ├── requirements.txt / pyproject.toml → Python 项目
              │   ├── 有 .venv 文件夹 → .venv\Scripts\python.exe xxx.py
              │   └── 没有 .venv → python -m venv .venv → 装依赖 → 运行
              │
              ├── pom.xml → Java Maven → mvn spring-boot:run
              │
              ├── go.mod → Go → go run main.go
              │
              ├── Cargo.toml → Rust → cargo run
              │
              └── Dockerfile → Docker → docker-compose up
```

---

## 七、你的项目具体对应关系

```
排课系统/
│
├── backend/                    ← Python 后端
│   ├── .venv/                  ← 虚拟环境（隔离的小房间）
│   ├── requirements.txt        ← Python 依赖清单
│   └── app/main.py             ← 入口文件
│
│   启动方式：
│   cd backend
│   .venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
│
└── frontend/                   ← Node.js 前端
    ├── node_modules/           ← 下载的依赖包
    ├── package.json            ← 项目配置和脚本定义
    └── vite.config.js          ← Vite 构建工具配置

    启动方式：
    cd frontend
    npm run dev
```

---

**记住核心原则：代码不会自己跑，总需要一个"运行器"。找到正确的运行器，按项目文档或标准惯例执行即可。**
