# 复制本文件为 backend/.env 后填写真实值；不要提交 .env 到 Git。

# PostgreSQL（生产环境将 DB_HOST 改为数据库内网地址或本机）
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=请填写强密码
DB_NAME=schedule_db

# 应用
APP_NAME=排课系统后端
DEBUG=false
API_PREFIX=/api/v1

# 桌面/便携模式（服务器部署请保持 false 或不设置）
USE_SQLITE=false
# SQLITE_PATH=
