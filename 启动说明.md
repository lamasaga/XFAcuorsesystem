# 排课系统启动说明

## 环境要求

- Python 3.11+（后端）
- Node.js + npm（前端）
- PostgreSQL 数据库（已配置好连接）

## 目录结构

```
XfaCourseSchedulingSystem/
├── backend/          # 后端服务
│   ├── .venv/        # Python 虚拟环境
│   └── app/
│       └── main.py   # FastAPI 入口
└── frontend/         # 前端服务
    ├── node_modules/
    └── package.json
```

---

## 启动后端

在 PowerShell 或 CMD 中执行：

```powershell
cd backend
.venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```

成功后显示：

```
INFO:     Uvicorn running on http://0.0.0.0:8001 (Press CTRL+C to quit)
INFO:     Application startup complete.
```

- **API 地址**：http://localhost:8001
- **API 文档**：http://localhost:8001/docs
- **停止**：按 `Ctrl + C`

---

## 启动前端

在另一个 PowerShell 或 CMD 窗口中执行：

```powershell
cd frontend
npm run dev
```

成功后显示：

```
VITE v5.4.21  ready in xxx ms
➜  Local:   http://localhost:3000/
```

- **页面地址**：http://localhost:3000
- **停止**：按 `Ctrl + C`

---

## 快速检查

两个服务都启动后，可以用浏览器访问：

| 服务 | 地址 | 预期结果 |
|------|------|----------|
| 后端 | http://localhost:8001 | 返回 JSON |
| 前端 | http://localhost:3000 | 显示排课系统页面 |

---

## 常见问题

### 1. 端口被占用

如果提示 `Address already in use`，说明端口已被其他进程占用。

- **后端（8001）**：换一个端口启动，例如 `--port 8002`
- **前端（3000）**：Vite 会自动切换到下一个可用端口，或手动指定 `npm run dev -- --port 3001`

### 2. 后端虚拟环境不存在

```powershell
cd backend
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
```

### 3. 前端依赖未安装

```powershell
cd frontend
npm install
```

### 4. 数据库连接失败

检查 `backend/.env` 文件中的数据库连接配置是否正确。

---

## 总结

只需要两个命令：

```powershell
# 窗口 1：启动后端
cd backend
.venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8001

# 窗口 2：启动前端
cd frontend
npm run dev
```

前后端各自独立运行，互不影响，按 `Ctrl + C` 即可单独停止。
