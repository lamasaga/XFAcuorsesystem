# Cursor AI 项目规则

## 项目背景
这是一个学校综合管理系统，包含：
- 排课系统（核心）
- 教务管理
- 学生管理
- 教室管理
- 用户认证

技术栈：
- 前端: Vue 3 + Vite + Element Plus + Pinia
- 后端: Python 3.10+ + FastAPI + SQLAlchemy
- 数据库: PostgreSQL + Redis

## 目录结构约定

### 后端模块结构
每个业务模块位于 `backend/app/modules/[模块名]/`，包含：
- `models.py` - SQLAlchemy 数据模型
- `schemas.py` - Pydantic 请求/响应模式
- `crud.py` - 数据库 CRUD 操作
- `service.py` - 业务逻辑
- `router.py` - FastAPI 路由

### 前端模块结构
每个业务模块位于 `frontend/src/modules/[模块名]/`，包含：
- `api.js` - API 调用封装
- `routes.js` - 路由配置
- `views/` - 页面组件目录

## 代码规范

### Python
- 使用 Python 3.10+ 语法特性
- 类型注解必须完整
- 使用 async/await 异步编程
- 遵循 PEP 8 规范
- Docstring 使用 Google 风格

### JavaScript/Vue
- 使用 Composition API + `<script setup>`
- 组件名使用 PascalCase
- 文件名使用 kebab-case
- Props 必须定义类型
- 事件名使用 kebab-case

### 命名规范
| 类型 | Python | JavaScript |
|------|--------|------------|
| 文件名 | snake_case.py | kebab-case.js/vue |
| 类名 | PascalCase | PascalCase |
| 函数/方法 | snake_case | camelCase |
| 变量 | snake_case | camelCase |
| 常量 | UPPER_SNAKE_CASE | UPPER_SNAKE_CASE |
| 数据库表 | snake_case (复数) | - |

## API 响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

分页响应：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [],
    "total": 100,
    "page": 1,
    "page_size": 20
  }
}
```

## 重要约定

1. **单文件行数限制**: 尽量保持单个文件 < 300 行
2. **错误处理**: 所有 API 必须有适当的错误处理
3. **类型安全**: 避免使用 any 类型
4. **日志记录**: 关键操作需要记录日志
5. **软删除**: 数据使用软删除，不物理删除

## 禁止事项

- ❌ 在代码中硬编码敏感信息（密码、密钥等）
- ❌ 在循环中进行数据库查询
- ❌ 省略错误处理
- ❌ 使用 `SELECT *`，明确指定需要的字段
- ❌ 前端直接存储敏感信息到 localStorage

## 常用模式

### 后端 - 创建 API 路由
```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.core.dependencies import get_db
from . import schemas, service

router = APIRouter()

@router.get("/", response_model=schemas.XxxListResponse)
async def get_list(
    page: int = 1,
    page_size: int = 20,
    db: Session = Depends(get_db)
):
    """获取列表"""
    return await service.get_list(db, page, page_size)
```

### 前端 - API 调用
```javascript
import request from '@/core/api'

export function getList(params) {
  return request.get('/api/v1/xxx', { params })
}

export function create(data) {
  return request.post('/api/v1/xxx', data)
}
```

### 前端 - Vue 组件
```vue
<template>
  <div class="xxx-page">
    <!-- 内容 -->
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { getList } from '../api'

const loading = ref(false)
const dataList = ref([])

const fetchData = async () => {
  loading.value = true
  try {
    const res = await getList()
    dataList.value = res.data.items
  } finally {
    loading.value = false
  }
}

onMounted(fetchData)
</script>

<style lang="scss" scoped>
.xxx-page {
  // 样式
}
</style>
```

## 参考文档

- 项目企划书: docs/项目企划书.md
- 系统架构: docs/系统架构设计.md
- UI设计: docs/UI设计方案.md
- API设计: docs/API设计.md
